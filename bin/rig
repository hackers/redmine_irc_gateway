#!/usr/bin/env ruby

$:.push File.expand_path('../../', __FILE__)
require 'lib/redmine_irc_gateway'

require 'rubygems'
require 'forever'

Forever.run do
  server = RedmineIRCGateway::Server.new
  log server.opts[:log] unless server.opts[:log]

  on_ready do
    server.run!
  end
end
